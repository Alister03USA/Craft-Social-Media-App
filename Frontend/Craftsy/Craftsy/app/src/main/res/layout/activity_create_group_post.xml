<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@color/blue_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Top Bar -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <Button
                android:id="@+id/backToGroupButton"
                style="@style/CraftsyButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="← Back to Group"
                android:backgroundTint="@color/blue_secondary"
                android:textSize="16sp" />

            <TextView
                android:text="Create Post"
                android:textColor="@color/text_black"
                android:textStyle="bold"
                android:textSize="22sp"
                android:layout_marginStart="20dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <!-- Image Preview -->
        <ImageView
            android:id="@+id/imagePreview"
            android:layout_width="match_parent"
            android:layout_height="240dp"
            android:layout_marginTop="24dp"
            android:scaleType="centerCrop"
            android:background="@drawable/craftsy_image_placeholder"
            android:contentDescription="Post Image Preview" />

        <!-- Select Image Button -->
        <Button
            android:id="@+id/selectImageButton"
            style="@style/CraftsyButton"
            android:text="Select Image"
            android:backgroundTint="@color/button_blue"
            android:layout_marginTop="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Post Text -->
        <EditText
            android:id="@+id/postTextInput"
            android:hint="Write something..."
            android:textColor="@color/text_black"
            android:background="@drawable/craftsy_card_bg"
            android:padding="12dp"
            android:textSize="16sp"
            android:gravity="top"
            android:inputType="textMultiLine"
            android:minHeight="140dp"
            android:layout_marginTop="24dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Submit Button -->
        <Button
            android:id="@+id/postButton"
            style="@style/CraftsyButton"
            android:text="Post"
            android:backgroundTint="@color/button_blue"
            android:layout_marginTop="24dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>
</ScrollView>
