stages:
  - mavenbuild
  - maventest
  
maven-build:            
  stage: mavenbuild
  tags:
    - alister_tag      # <-- change to your runner's tag
  script:
    - cd Backend/Craftsy       # change 'Backend' to to where you have the pom.xml (do not add / in the beginning)
    - mvn package
  only:
    refs:
      - alister_CICD
    changes:
      - Backend/**/*


maven-test:             
  stage: maventest
  tags:
    - alister_tag     # <-- change to your runner's tag
  script:
    - cd Backend/Craftsy       # change 'Backend' to to where you have the pom.xml (do not add / in the beginning)
    - mvn test
  only:
    refs:
      - alister_CICD
    changes:
      - Backend/**/*
